jdk:
  - openjdk17

install:
  - echo "org.gradle.configuration-cache=false" >> gradle.properties
  - ./gradlew clean bundleStripeFatAar --no-configuration-cache
  - mkdir -p ./build/libs
  - cp build/outputs/fat-aar/stripe-android-lim.aar ./build/libs/stripe-android-lim.aar
  - ls -R build