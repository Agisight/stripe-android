jdk:
  - openjdk17

install:
  - echo "org.gradle.configuration-cache=false" >> gradle.properties
  - ./gradlew clean bundleStripeFatAar --no-configuration-cache
  - mkdir -p ./libs
  - cp build/build/outputs/fat-aar/stripe-android-lim.aar ./libs/stripe-android-lim.aar
