jdk:
  - openjdk17

install:
  - echo "org.gradle.configuration-cache=false" >> gradle.properties
  - ./gradlew clean bundleStripeFatAar --no-configuration-cache
  - mkdir -p ./libs
  - if [ -f build/outputs/fat-aar/stripe-android-lim.aar ]; then cp build/outputs/fat-aar/stripe-android-lim.aar ./libs/; fi
  - if [ -f build/build/outputs/fat-aar/stripe-android-lim.aar ]; then cp build/build/outputs/fat-aar/stripe-android-lim.aar ./libs/; fi
  - ls -R ./libs
